@section scripts {
    <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.2.1.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.3/js/jquery.dataTables.min.js"></script>
    <script>
        $(document).ready(function () {

            const aData = [];

            for (let i = 1; i <= 10; i++) {
                const companyObject = {
                    ProductName: "ProductName: " + i,
                    ProductCode: "ProductCode: " + i,
                    Cost: "Cost: " + i
                }
                aData.push(companyObject);
            }
            $.ajax({
                type: "POST",
                url: '/AjaxRedirectToAnotherAction/CreateOrderWithButtonCheckedProducts',
                dataType: 'json',
                data: { "ProductViewModel": aData },
                success: function (result) {
                   // alert("here");
                   //// window.location.href = "@Url.Action("CreateOrderWithButtonCheckedProduct", " AjaxRedirectToAnotherAction")?result=" + response.message;
                   // window.location.href = "@Url.Action("CreateOrderWithButtonCheckedProduct", " AjaxRedirectToAnotherAction")?result=" + aData;

                }

            });


        });
    </script>
}
