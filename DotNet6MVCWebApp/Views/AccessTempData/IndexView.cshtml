<div class="row">
    <div class="col-sm-4">
        <div class="card-body b">

            @using (Html.BeginForm("HandleButtonOn", "AccessTempData"))
            {
                <button type="submit" name="ButtonOn" class="btn btn-success btn-lg btn-block" value="1">High Power</button>
            }
        </div>
    </div>
</div>

@*<form asp-action="NewTaxes" asp-controller="UserLog" method="post" autocomplete="off">
    <div class="form-row">
        <div class="form-group col-md-6">
            <label>Tax Name</label>
            <input type="text" asp-for="@Model.Tax.TaxName" class="form-control" id="TaxName" placeholder="Tax Name" />
            <span class="text-danger" asp-validation-for="@Model.Tax.TaxName"></span>
        </div>
        <br />
        <div class="form-group row">
            <div class="col-sm-10">
                <button type="submit" class="btn btn-primary">CREATE</button>
            </div>
        </div>
    </div>

</form>*@