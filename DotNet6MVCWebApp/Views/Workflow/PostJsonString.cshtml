@section scripts {
    <script src="https://ajax.aspnetcdn.com/ajax/jQuery/jquery-3.2.1.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.3/js/jquery.dataTables.min.js"></script>
    <script>
        $(document).ready(function () {

            //$.ajax({
            //    url: "/Workflow/OnPostDelete",
            //    type: "DELETE",
            //    data: {
            //        'ID': 1
            //    },
            //    dataType: "json",
            //    success: function (response) {
            //        console.log(response);

            //        window.location.href = "@Url.Action("AnotherIndex", "Workflow")?message=" + response.message;
            //        if (!response.success) {
            //            alert("here1");
            //        }
            //    },
            //    error: function (xhr, error, thrown) {
            //        alert("here2" + xhr.responseText);
            //    }
            //});

            var productId = "Pro:";
            var colorId = "Col:";
            var shippingId = "Shi:";
            var colorCount = "Cnt:";
            
            var shippingArray = [];
            for(let i=0; i<=150;i++){
                var items = {
                    ProductId: productId+i,
                    ColorId: colorId+i,
                    ShippigId: shippingId+i,
                    Count: colorCount+i
                }
                shippingArray.push(items);
            }

            console.log(shippingArray);

            var json = JSON.stringify(shippingArray);
            var url = '@Url.Action("ShippingFinishDeleteItem","Workflow")';
            $.ajax({
                url: url,
                type: "POST",
                traditional: true,
                datatype: "JSON",
                async: true,
                data: {
                    "body": json
                },
                success: function () {

                }
            });

        });
    </script>
}
