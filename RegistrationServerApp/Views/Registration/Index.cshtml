@model IEnumerable<RegistrationServerApp.Models.User>

@{
    ViewData["Title"] = "Index";
}


<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserEmail)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LoginId)
            </th>
            <th>
                Registration Details
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserEmail)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.LoginId)
                </td>
                <td>
                    <a asp-action="EditDriver" class="btn btn-primary" asp-route-memberId="@item.UserName">Details</a> |  <a asp-action="EditDriver" class="btn btn-warning" asp-route-driverId="@item.UserName">Edit</a>
                </td>
            </tr>
        }
    </tbody>
</table>


